NomV = 3.7;
EOCV = UGas;
EODV = CutOff;
CRate = 0.2;
EOCRate = 0.02;

limit(V > EOCV, error(1));
limit(V < EODV, error(1));

cycle(10) {
	charge(CRate ACn1, V = EOCV, limit(ACn1 < EOCRate));
	discharge(CRate ACn1, limit(V <= EODV));
}
charge(1 ACn5, limit(V >= NomV));
