stages:
  - build

pages:
  stage: build
  image: node:14.19.3
  artifacts:
    paths:
      - public
  cache:
    paths:
      - .npm/
  before_script:
    - npm ci --cache .npm --prefer-offline
    - git submodule init && git submodule update
  script:
    - ./make.sh
