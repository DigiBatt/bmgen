limit(V > 4.3, error(1));
limit(V < 2.5, error(1));
limit(PBOC1 > 65, error(1));

// Variablen des Kapazitätstests
CEla = 1.7;
CLad = 1.7;
DODAh = 3.4;
AnodeAh = 3.4;
EOCycV = 3;

pause(limit(5 min));

cycle(50) {
  discharge(A = CEla, limit(V <= EODV));
  charge(A = CLad, limit(V >= EOCV));
}